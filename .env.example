# =============================================================================
# Provenance Analysis Docker Compose Environment Variables
# =============================================================================
# Copy this file to .env and modify as needed
#
# This compose assumes CBIR system is already running (from cbir-system directory)

# =============================================================================
# Service Port
# =============================================================================
# Port for provenance service (default: 8002 to avoid conflict with CBIR on 8001)
PROVENANCE_PORT=8002

# =============================================================================
# CBIR Connection
# =============================================================================
# URL of the CBIR service (default connects to host machine's port 8001)
CBIR_ENDPOINT=http://host.docker.internal:8001

# User ID for CBIR isolation (multi-tenant support)
CBIR_USER_ID=provenance_service

# =============================================================================
# Data Path Configuration
# =============================================================================
# Path on your HOST machine where images are stored
# This will be mounted as /workspace in the container
DATA_PATH=/path/to/your/image/data

# Path mapping for file paths
# If your application sends paths like /home/user/images/img.png
# but the container sees them as /workspace/img.png, set:
LOCAL_DATA_PATH=/home/user/images
# REMOTE_DATA_PATH is always /workspace (set in docker-compose)

# =============================================================================
# Logging
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
